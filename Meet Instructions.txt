if any changes made to TicketDataToXML.py, replace this file on the download computer with the new one.

in COCScraper.properties on the download computer:
	verify correct values for server
	verify correct values for xmlwriterdir and xmlwriterpy

start the server on port 80 writing logs to file:
(if sudo is not available, server runs on port 5000)
	sudo npm start &> ./logs/meet_name_01.log

refresh the database using the robomongo gui:
	drop collection for all collections
	load the race configs
		python ./client/postdata.py http://localhost races ./configdata/races.json
	load the org configs
		python ./client/postdata.py http://localhost orgs ./configdata/orgs.json


<at this point, the server is ready to take data POSTed from postdata or ticketdata>




