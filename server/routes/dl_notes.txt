Steps:

Determine if runner already exists

lookup orgID based on org code
save runner with name string, org ID code (no result ID)
get the just-created runnerID
  update the org with this new runnerID

lookup the raceID based on classResult level code/info
save the result with race ID, runner ID, and strings, but no punchIDs
get the just-created resultID
  update the runner with the resultID
  update the race with the resultID

save each punch with the resultID
get the punchID
  udpate the result with the punchID
